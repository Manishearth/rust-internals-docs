initSidebarItems({"struct":[["CoerceMany","CoerceMany encapsulates the pattern you should use when you have many expressions that are all getting coerced to a common type. This arises, for example, when you have a match (the result of each arm is coerced to a common type). It also arises in less obvious places, such as when you have many `break foo` expressions that target the same loop, or the various `return` expressions in a function."]],"trait":[["AsCoercionSite","Something that can be converted into an expression to which we can apply a coercion."]],"type":[["DynamicCoerceMany","The type of a `CoerceMany` that is storing up the expressions into a buffer. We use this in `check/mod.rs` for things like `break`."]]});