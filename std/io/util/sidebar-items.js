initSidebarItems({"fn":[["copy","Copies the entire contents of a reader into a writer.This function will continuously read data from `reader` and then write it into `writer` in a streaming fashion until `reader` returns EOF.On success, the total number of bytes that were copied from `reader` to `writer` is returned.ErrorsThis function will return an error immediately if any call to `read` or `write` returns an error. All instances of `ErrorKind::Interrupted` are handled by this function and the underlying operation is retried.Examples"],["empty","Constructs a new handle to an empty reader.All reads from the returned reader will return `Ok(0)`.ExamplesA slightly sad example of not reading anything into a buffer:"],["repeat","Creates an instance of a reader that infinitely repeats one byte.All reads from this reader will succeed by filling the specified buffer with the given byte."],["sink","Creates an instance of a writer which will successfully consume all data.All calls to `write` on the returned instance will return `Ok(buf.len())` and the contents of the buffer will not be inspected."]],"struct":[["Empty","A reader which is always at EOF.This struct is generally created by calling `empty()`. Please see the documentation of `empty()` for more details."],["Repeat","A reader which yields one byte over and over and over and over and over and...This struct is generally created by calling `repeat()`. Please see the documentation of `repeat()` for more details."],["Sink","A writer which will move data into the void.This struct is generally created by calling `sink()`. Please see the documentation of `sink()` for more details."]]});