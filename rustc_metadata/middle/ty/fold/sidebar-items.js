initSidebarItems({"fn":[["shift_region",""],["shift_regions",""]],"struct":[["BottomUpFolder",""],["RegionFolder","Folds over the substructure of a type, visiting its component types and all regions that occur *free* within it.That is, `Ty` can contain function or method types that bind regions at the call site (`ReLateBound`), and occurrences of regions (aka \"lifetimes\") that are bound within a type are not visited by this folder; only regions that occur free will be visited by `fld_r`."]],"trait":[["TypeFoldable","The TypeFoldable trait is implemented for every type that can be folded. Basically, every type that has a corresponding method in TypeFolder."],["TypeFolder","The TypeFolder trait defines the actual *folding*. There is a method defined for every foldable type. Each of these has a default implementation that does an \"identity\" fold. Within each identity fold, it should invoke `foo.fold_with(self)` to fold each sub-item."],["TypeVisitor",""]]});