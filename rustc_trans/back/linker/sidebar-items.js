initSidebarItems({"struct":[["GnuLinker",""],["MsvcLinker",""]],"trait":[["Linker","Linker abstraction used by back::link to build up the command to invoke a linker.This trait is the total list of requirements needed by `back::link` and represents the meaning of each option being passed down. This trait is then used to dispatch on whether a GNU-like linker (generally `ld.exe`) or an MSVC linker (e.g. `link.exe`) is being used."]]});